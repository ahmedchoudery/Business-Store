{
    "name": "business-store-monorepo",
    "version": "1.0.0",
    "private": true,
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.4.5",
        "express": "^4.19.2",
        "express-validator": "^7.1.0",
        "helmet": "^7.1.0",
        "mongoose": "^8.4.1"
    },
    "engines": {
        "node": "20.x"
    },
    "devDependencies": {
        "shx": "^0.4.0"
    },
    "scripts": {
        "install-all": "npm install && cd client && npm install",
        "build": "cd client && npm install && npm run build && cd .. && npx shx cp -r client/dist/* .",
        "start": "node api/index.js"
    }
}